- name: 🔍 Confirm script path from cloned action
  shell: pwsh
  run: |
    $base = "$env:GITHUB_ACTION_PATH"
    if (-not $base) {
      $base = "C:\actions-runner\_work\_actions\abc-internal\dacpac-actions\v1.0.2"
    }

    $scriptPath = Join-Path $base "OD-GHA-MIGRATION\od-gha-files\sql-actions.ps1"
    Write-Host "Looking for script at: $scriptPath"

    if (Test-Path $scriptPath) {
      Write-Host "✅ Script found at: $scriptPath"
    } else {
      Write-Host "❌ Script not found."
      Get-ChildItem -Recurse -Path $base | Where-Object { $_.Name -eq "sql-actions.ps1" }
    }
