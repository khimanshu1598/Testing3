# Define deployment actions (Choose one: DeployReport, Script, Publish)
$Action = "DeployReport"

# Path to the DACPAC file to be deployed
$DacpacFilePath = "C:\abc\releases\EmployeeInvitation\Employee.Invitation.Database.Build.dacpac"

# Path where artifacts will be downloaded
$ArtifactDownloadFilePath = "C:\abc\releases\databases\EmployeeInvitation"

# Database name
$DatabaseName = "EmployeeInvitation"

# Path to the Publish Profile XML
$PublishProfileFilePath = "C:\abc\releases\databases\EmployeeInvitation\abc-uat.publish.xml"

# Publish profile file name
$PublishProfileFileName = "abc-uat.publish.xml"

# Target SQL Server instance
$TargetServers = "employeeinvitation.db.livestage6.test.abc.com"

# Path to the deployment report file
$DeployReportFilePath = "C:\abc\releases\databases\EmployeeInvitation\deployreports\LS6_Global-01.EmployeeInvitation.$($OctopusReleaseNumber).deployreport.xml"

# Path to SqlPackage.exe (Ensure this version exists on the target machine)
$SqlPackagePath = "C:\Program Files (x86)\Microsoft SQL Server\130\DAC\bin\SqlPackage.exe"

# Additional deployment properties for SQLPackage
$SqlPackageDeployProperties = @{
    "AdditionalDeploymentContributorArguments" = "CreateIndexOperationalPropsModifier.Online=ON;CreateIndexOperationalPropsModifier.MAXDOP=2"
}

# Boolean Flags
$AddArtifactsToRelease = ($AddArtifactsToRelease -eq "true")
$UsingClusterNames = ($UsingClusterNames -eq "true")
$UseServiceAccount = ($UseServiceAccount -eq "true")
$UseMultiSubnetFailover = ($UseMultiSubnetFailover -eq "true")

# SQL Service Account Credentials (Ensure secure handling of credentials)
$ServiceAccountUserName = ""  # Placeholder for service account username
$ServiceAccountPassword = ""  # Placeholder for service account password

# SQLCMD Variables (Expected to be referenced in another script)
$SqlCmdVariables = @{}  # Example: @{ "Variable1" = "Value1"; "Variable2" = "Value2" }

# SQLPackage Parameters (Expected to be referenced in another script)
$SqlPackageParameters = @{}  # Example: @{ "Parameter1" = "Value1"; "Parameter2" = "Value2" }

# Octopus Environment Variables
$OctopusEnvironmentName = "LS6 Global - 01"
$OctopusReleaseNumber = "Build Number"
